import React, { ReactElement } from 'react'

interface Props {
    
}

function BoilerPlates({}: Props): ReactElement {
    return (
        <div>
            BoilerPlates
        </div>
    )
}

export default BoilerPlates
