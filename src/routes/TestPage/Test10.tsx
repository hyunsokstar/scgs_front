import { Box, Button, Checkbox } from "@chakra-ui/react";
import React, { useState } from "react";

interface Props {}

const Test10: React.FC<Props> = ({}) => {
  return (
    <Box>
      <Checkbox size="lg" colorScheme="green" >
      </Checkbox>
    </Box>
  );
};

export default Test10;
