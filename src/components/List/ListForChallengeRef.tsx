import React from 'react'
import {
    Button,
    Modal,
    ModalOverlay,
    ModalContent,
    ModalHeader,
    ModalCloseButton,
    ModalBody,
    ModalFooter,
    Table,
    Thead,
    Tbody,
    Tr,
    Th,
    Td,
    Input,
    Link,
    IconButton,
    Box,
} from "@chakra-ui/react";
import { FaEdit, FaCheck, FaTimes, FaTrash } from "react-icons/fa";

interface IProps {
    dataForChallengeRefs: 
}

const ListForChallengeRef = ({
    dataForChallengeRefs
}: IProps) => {
    return (
        <Box>
            
        </Box>
    )
}

export default ListForChallengeRef
